-In generale la mia idea è quella di fare un menù semplice in go con interfaccia da terminale, sviluppo tutto così poi l'interfaccia grafica sarà solo la stessa cosa potenziata, quindi per capire come funziona tutto guardare client.go
-Codice per fare hash del pwd in c#:
                using BCrypt.Net;

                public class PasswordHasher
                {
                    public static string HashPassword(string password)
                    {
                        return BCrypt.Net.BCrypt.HashPassword(password);
                    }

                    public static bool VerifyPassword(string password, string hashedPassword)
                    {
                        return BCrypt.Net.BCrypt.Verify(password, hashedPassword);
                    }
                }

